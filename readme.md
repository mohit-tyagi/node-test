# Steps to install `node version 4.x and above `
- npm install
- bower install
- npm start

# The Marco Polo Game
- open `index.html` file.

# Math game (Implementing a User Story)
- open `math.html` file.